---
source_directory:   client
build_directory:    lib
builds:
  min:
    shrink_vars:    true
    private:        true
packages:
  faye:
    - faye
    - util/class
    - util/event
    - util/uri
    - util/xhr
    - channel
    - transport
    - client
    - util/json

