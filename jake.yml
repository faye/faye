---
source_directory:   lib/javascript
build_directory:    lib
builds:
  min:
    shrink_vars:    true
    private:        true
packages:
  faye:
    - faye
    - util/class
    - util/event
    - util/uri
    - util/xhr
    - transport
    - client
    - util/json

