---
source_directory:   javascript
build_directory:    build
builds:
  src:
    packer:         false
  min:
    shrink_vars:    true
    private:        true
packages:
  core:
    - faye
    - util/class
  server:
    extends: core
    files:
      - server
      - node_adapter
  client:
    extends: core
    files:
      - util/event
      - util/uri
      - util/xhr
      - channel
      - transport
      - client
      - util/json

