# -*- ruby -*-

require 'rubygems'
require 'hoe'
require './lib/faye.rb'

Hoe.spec('faye') do |p|
  # p.rubyforge_name = 'fayex' # if different than lowercase project name
  p.developer('James Coglan', 'jcoglan@googlemail.com')
  p.extra_deps = %w[eventmachine rack json oyster]
end

# vim: syntax=Ruby
